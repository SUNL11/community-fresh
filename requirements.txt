# 生鲜 AI 品控 — Python 依赖

# 核心框架
gradio==3.50.2
numpy>=1.24.0,<2.0
opencv-python-headless==4.10.0.84
pillow==10.4.0

# 视觉推理（YOLOv11n）
# 视觉推理（YOLOv11n）/ 用户环境实测 ultralytics 8.4.102 正常
ultralytics>=8.3.0
torch>=2.0.0

# LLM API 客户端
httpx==0.27.2
python-dotenv>=1.0.0

# 数据持久化
# sqlite3 为 Python 内置，无需额外安装

# 可选：报表导出（扩展功能）
# openpyxl==3.1.5
# reportlab==4.2.0
